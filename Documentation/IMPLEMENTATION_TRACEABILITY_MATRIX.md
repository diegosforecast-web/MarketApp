# Implementation Traceability Matrix v1.0

| Change ID | Engineering Design Sections | Repository File(s) | Purpose | Validation | Regression Risk | Status | Repository Commit | Review Outcome |
|---|---|---|---|---|---|---|---|---|
| PE1-001 | §§5, 7, 12 | `src/MarketApp/backend/services/entitlement_service.py` | Confirm existing entitlement implementation satisfies Product Evolution authorization requirements. | Existing authorization and subscription toolkit tests. | Low | Validated — no code change required | Existing implementation | Existing implementation retained |
| PE1-003A | §§6, 7, 12 | `src/MarketApp/backend/services/response_builder.py`; `src/MarketApp/backend/services/prediction_service.py`; `src/MarketApp/backend/tests/test_response_builder.py` | Introduce and integrate the behavior-preserving ResponseBuilder abstraction. | `pytest -q tests/test_response_builder.py tests/test_subscription_toolkit.py` | Low | Validated | Existing implementation | Approved by repository validation |
| PE1-003B | §§6, 7, 12 | `src/MarketApp/backend/services/prediction_service.py` | Verify all PredictionResponse construction delegates to ResponseBuilder. | Repository inspection plus targeted regression suite. | Low | Validated | Existing implementation | Closed as already implemented |
| PE1-004 | §§6, 7, 12 | `src/MarketApp/backend/schemas/response.py`; `src/MarketApp/backend/services/response_builder.py`; `src/MarketApp/backend/services/prediction_service.py`; `src/MarketApp/backend/tests/test_response_builder.py` | Introduce the additive Forecast Collection while preserving the existing forecast path. | Builder tests; subscription toolkit regression; Python compilation; serialization inspection. | Medium | Validated | Existing implementation | Chief Approved |
| PE1-005 | §§5, 6, 7, 12 | `src/MarketApp/backend/services/response_builder.py`; `src/MarketApp/backend/tests/test_response_builder.py` | Apply capability-based filtering after forecast construction without modifying forecast computation. | Python compilation; builder tests; subscription toolkit regression. | Low | Validated | Existing implementation | Chief Approved |
| PE1-006 | §§5, 6, 7, 12; Workstream E | `src/MarketApp/backend/app.py`; `src/MarketApp/backend/endpoints/daily_selection.py`; `src/MarketApp/backend/schemas/daily_selection.py`; `src/MarketApp/backend/services/daily_selection_service.py`; `src/MarketApp/backend/services/entitlement_service.py`; `src/MarketApp/backend/tests/test_daily_selection_service.py`; `src/MarketApp/backend/tests/test_daily_selection_entitlements.py`; `src/MarketApp/backend/tests/test_daily_selection_api.py`; `supabase/migrations/20260730_add_premium_daily_selections.sql` | Add persistent backend-authoritative Premium Daily Selection with one immutable choice per user and market day while preserving Gold/Administrator simultaneous Forecast Collection behavior. | Python compilation; 40 targeted backend/API/regression tests; migration validation; merge validation; release tag verification. | Medium | Validated — Chief Approved | Merge commit `7c955905`; backend commit `4acf4f25`; tag `v2.0-pe1-backend-complete` | Backend milestone accepted, merged into `main`, tagged and released. |
| PE1-007A | Forecast Controls and Premium Daily Selection | `frontend/src/components/Dashboard.jsx`; `frontend/src/components/Dashboard.css`; `frontend/src/components/PremiumDailySelection.jsx` | Frontend repository inspection complete and additive Premium Daily Selection implemented in the Dashboard forecast workspace using authenticated backend responses as authoritative state, with no browser-side entitlement or locking rules. | Frontend production build passed; authenticated GET/PUT contract validation completed; locked-state and independent request-state verification completed; runtime validation completed. | Low | Chief Approved — Merged | Implementation commit `3301ebb2`; merged into `main` via merge commit `42db37a5` | Frontend integration approved following runtime verification, Premium authorization validation, persistence verification, production build success, and merged into `main`. |

| PE1-007B | Forecast Presentation | `src/MarketApp/backend/schemas/response.py`; `src/MarketApp/backend/services/response_builder.py`; `src/MarketApp/backend/services/prediction_service.py`; `src/MarketApp/backend/tests/test_response_builder.py`; `frontend/src/components/Dashboard.jsx`; `frontend/src/components/Dashboard.css` | Connect capability filtering and Premium Daily Selection to the live forecast response through an additive backend-authoritative presentation contract while preserving `forecast_price` and existing API compatibility. | Python compilation passed; 45 backend tests passed; targeted and full regression validation completed; Explorer/Standard legacy presentation verified; Premium selected and missing-selection scenarios verified; Gold simultaneous presentation verified; filtering and serialization validation completed; frontend production build passed; Dashboard fallback validation completed; PE1-007B-specific lint issue resolved; one pre-existing out-of-scope Dashboard lint issue remains. | Medium | Chief Approved — Merged | Implementation commit `679de700`; merged into `main` via merge commit `42db37a5` | Merged into `main`. Administrator public-path support deferred because no authoritative production Administrator identity source exists in the verified repository. Gold is the only public simultaneous Forecast Collection path. Administrator simultaneous presentation remains restricted to trusted internal service invocation. All public authentication paths fail closed with `administrator=False`. Backend remains authoritative for presentation resolution and capability filtering. |
| PE1-007C | Pricing, Upgrade Experience, and UI Polish | `frontend/src/components/Dashboard.jsx`; `frontend/src/components/Dashboard.css`; `frontend/src/components/SettingsPanel.jsx`; `frontend/src/components/SettingsPanel.css` | Improve pricing clarity, upgrade discoverability, subscription comparison, messaging consistency, and visual hierarchy using the existing authenticated billing and entitlement architecture. Preserve backend authority, Stripe lifecycle, subscription enforcement, routing, authentication, and forecast behavior. | Increment 1: frontend production build passed; Explorer, Standard, Premium, and Gold pricing states verified; billing entry points unchanged; current-plan behavior and responsive presentation verified. Increment 2: frontend production build passed; Explorer, Standard, Premium, and Gold messaging verified; billing actions unchanged; responsive text wrapping verified; no new lint errors introduced; one pre-existing out-of-scope Dashboard lint issue remains. Increment 3: repository inspection completed; no materially distinct implementation gap identified; implementation not required; PE1-007C closed after Increments 1 and 2. | Low | Closed — Chief Approved | Increment 1 commit `2689ad72`; Increment 2 commit `4ca69e9d`; Increment 3 repository inspection (no implementation required) | Pricing Cards (Increment 1) and Subscription Messaging (Increment 2) completed, validated, and approved by the Chief Engineer. Increment 3 repository inspection confirmed no additional production work was warranted. Backend, API, billing, Stripe, authentication, entitlement, routing, and forecast behavior remain unchanged. PE1-007C closed. |
